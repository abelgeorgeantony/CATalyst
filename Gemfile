source "https://rubygems.org"

# This gem ensures your local environment matches GitHub Pages' build environment
gem "github-pages", group: :jekyll_plugins

# If you want to use the default Jekyll theme (Minima)
gem "minima", "~> 2.5"

# The feed plugin we added to your _config.yml
group :jekyll_plugins do
  gem "jekyll-feed", "~> 0.12"
end